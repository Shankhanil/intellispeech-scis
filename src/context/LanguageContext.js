import { createContext } from 'react';

/**
 * Available languages
 * English ==> default
 * Bengali
 */
export const languageList = {
    'english':'English',
    'bengali':'বাংলা',
};

export const LanguageContext = createContext();